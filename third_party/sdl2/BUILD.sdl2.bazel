load("@rules_foreign_cc//foreign_cc:defs.bzl", "cmake")

filegroup(
    name = "all_srcs",
    srcs = glob(["**"]),
    visibility = ["//visibility:public"],
)

cmake(
    name = "sdl2",
    build_args = ["-DALSA=ON"],
    defines = [
        "ALSA=ON",
        # Add other options as needed
    ],
    # configure_in_place = True,

    # configure_options = [],
    env = select({
        "@platforms//os:macos": {
            "AR": "",
            "PERL": "$$EXT_BUILD_ROOT$$/$(PERL)",
        },
        "//conditions:default": {
            "PERL": "$$EXT_BUILD_ROOT$$/$(PERL)",
            "ALSA_INCLUDE_DIR": "$$EXT_BUILD_DEPS/alsa_dynamic_link/include",
            "ALSA_LIBRARY": "$$EXT_BUILD_DEPS/alsa_dynamic_link/lib/libasound.so",
        },
    }),
    lib_name = "sdl2",
    lib_source = ":all_srcs",
    out_include_dir = "include/SDL2",
    out_shared_libs = [
        "libSDL2.so",
        "libSDL2-2.0.so.0",
    ],
    targets = [],
    toolchains = ["@rules_perl//:current_toolchain"],
    visibility = ["//visibility:public"],
    deps = [
        "@alsa//:alsa_dynamic_link",
        "@alsa//:alsa_static_link",
    ],
)
