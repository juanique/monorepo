load("//bazel/js:defs.bzl", "ts_binary")
load("//bazel/oci:defs.bzl", "js_image")
load("//bazel/oci/testing:defs.bzl", "container_output_test")

ts_binary(
    name = "relative_imports",
    srcs = ["main.ts"],
    deps = ["//bazel/js/tests/relative_imports/formatting"],
)

js_image(
    name = "image",
    binary = ":relative_imports",
)

container_output_test(
    name = "test",
    expected_output_string = "Formatted Name: ✨ John Doe ✨",
    image = ":image",
)
