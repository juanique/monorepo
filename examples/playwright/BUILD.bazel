load("//bazel/playwright:defs.bzl", "playwright_test")

# TODO: Add playwright support to gazelle
# gazelle:exclude ./

playwright_test(
    name = "test",
    srcs = ["example.spec.ts"],
    deps = [
        "//salsa/testing/playwright",
    ],
)
