load("@pip_deps//:requirements.bzl", "requirement")
load("//:bazel/python.bzl", "pytest_test")

py_binary(
    name = "version",
    srcs = ["version.py"],
)

py_binary(
    name = "tree",
    srcs = ["tree.py"],
    deps = [
        requirement("rich"),
    ],
)

pytest_test(
    name = "test_pytest",
    srcs = ["test_pytest.py"],
)
