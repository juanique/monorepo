load("@pip_deps//:requirements.bzl", "requirement")

py_binary(
    name = "version",
    srcs = ["version.py"],
)

py_binary(
    name = "tree",
    srcs = ["tree.py"],
    deps = [
        requirement("rich"),
    ],
)
