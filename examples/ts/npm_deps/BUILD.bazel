load("@//bazel/ts:defs.bzl", "ts_library")
load("//bazel/ts:defs.bzl", "ts_binary")

ts_binary(
    name = "main",
    srcs = ["main.ts"],
    deps = [
        "//:node_modules/@types/figlet",
        "//:node_modules/chalk",
        "//:node_modules/figlet",
    ],
)

ts_library(
    name = "ts",
    srcs = ["main.ts"],
    deps = [
        "//:node_modules/@types/figlet",
        "//:node_modules/chalk",
        "//:node_modules/figlet",
    ],
)
